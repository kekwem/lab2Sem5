function Click(str) {
    var input = document.getElementsByTagName('input')[0];
    input.value += str;
}
function Clear() {
    var input = document.getElementsByTagName('input')[0];
    input.value = "";
}
function Calculate() {
    var input = document.getElementsByTagName('input')[0];
}
